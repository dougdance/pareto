<%= form_for(@dd_table) do |f| %>
  <% if @dd_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dd_table.errors.count, "error") %> prohibited this dd_table from being saved:</h2>

      <ul>
      <% @dd_table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit @submit_text %>
  </div>
<% end %>
