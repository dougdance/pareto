<%= form_for(@dd_row, :url => dd_table_dd_rows_path(@dd_table)) do |f| %>
  <% if @dd_row.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dd_row.errors.count, "error") %> prohibited this dd_row from being saved:</h2>

      <ul>
      <% @dd_row.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% @dd_string_columns.each do |column| %>
    <div class
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
