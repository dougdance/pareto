<%= form_for(@dd_string_column) do |f| %>
  <% if @dd_string_column.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dd_string_column.errors.count, "error") %> prohibited this dd_string_column from being saved:</h2>

      <ul>
      <% @dd_string_column.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dd_table_id %><br />
    <%= collection_select :dd_string_column, :dd_table_id,
        @dd_tables, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
